# Design System

## Register

product

## Visual Direction

清晰、可靠、克制。主界面沿用青绿色作为操作与状态强调色，文档页使用白色内容面、冷灰分层和深色代码块，优先保证扫描和复制效率。

## Color Tokens

- `--accent`: #0ea99c，用于主操作、当前目录和成功状态。
- `--accent-strong`: #087f78，用于链接和强调文字。
- `--bg`: #f7fafb，页面底色。
- `--surface`: #ffffff，内容面和章节容器。
- `--surface-soft`: #f1f6f6，表头、辅助面和代码行内标记。
- `--ink`: #172231，标题和正文主要文字。
- `--ink-soft`: #405064，正文次级文字。
- `--muted`: #718096，说明文字和目录辅助信息。
- `--line`: #dfe7e9，分隔线和边框。
- `--code-bg`: #182536，代码块背景。

## Typography

使用系统无衬线字体，正文 15px，章节标题约 1.4rem，页面标题使用有限的响应式缩放。代码使用系统等宽字体。正文控制在易读行长，代码允许横向滚动。

## Components

- 固定侧栏目录：桌面端常驻，900px 以下切换为抽屉。
- 章节容器：12px 圆角、细边框和轻阴影，不嵌套卡片。
- 提示框：信息、警告、危险三种语义颜色，图标与文字并列。
- 代码块：深色背景，右上角提供复制操作和复制反馈。
- 标签页：用于 Windows 与 macOS / Linux 内容切换。
- 折叠问答：使用原生 `details`，支持键盘和无脚本阅读。

## Responsive Rules

900px 以下收起目录；620px 以下将流程、配置链接和支持信息改为单列。表格保持最小宽度并允许横向滚动，避免字段挤压或溢出。

## Accessibility

正文和控件保持可读对比度；目录、标签页、复制按钮和折叠问答均使用语义元素；动画遵守 `prefers-reduced-motion`。
