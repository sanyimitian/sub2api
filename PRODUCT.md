# Product

## Register

product

## Users

面向个人开发者、AI 工具用户和小型团队。用户通常已经在使用 Claude Code、Codex、ChatGPT 或其他兼容客户端，希望用一个稳定的入口统一管理模型调用、API 密钥和用量。

## Product Purpose

Sub2API 是一个 AI API 网关和订阅配额分发平台。它把多个上游模型服务统一为可管理的 API 入口，并提供账号池、分组、密钥、计费、配额和运维能力。成功标准是：用户能快速完成接入，管理员能清楚控制资源，调用过程稳定且可追踪。

## Brand Personality

清晰、可靠、克制。界面和文档应让第一次接入的用户少走弯路，同时让有经验的开发者能快速定位字段、地址和命令。

## Anti-references

不使用营销落地页式的大段装饰、过度圆润的组件、无法验证的功能承诺或与实际配置不一致的示例。文档不应隐藏关键前置条件，也不应把警告信息做成难以扫描的长段落。

## Design Principles

1. 先给可执行结论，再解释原因。
2. 所有示例都对应真实字段、真实路径和真实操作顺序。
3. 将高风险配置（地址、密钥、分组、额度）放在用户能快速扫描的位置。
4. 复杂流程拆成短步骤，并提供可回退的排查路径。

## Accessibility & Inclusion

以 WCAG 2.1 AA 为目标，保证正文和代码块有足够对比度，目录和标签可用键盘操作，移动端不依赖悬停，动画支持 `prefers-reduced-motion`。
