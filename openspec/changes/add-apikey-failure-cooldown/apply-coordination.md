# Apply Coordination Record

## Baseline

- Change: `add-apikey-failure-cooldown`
- Schema: `spec-driven`
- Source branch: `main`
- Original HEAD: `d2965807544aad1816c7c9bfec073bddecabf2cb`
- Starting workspace: no tracked changes; five untracked OpenSpec artifact files under this change
- Pre-apply backup branch: `add-apikey-failure-cooldown-pre-apply-backup-20260827181100`
- Pre-apply backup worktree: `/home/pan/桌面/code/add-apikey-failure-cooldown-实施前备份-20260827181100`
- Backup verification: tracked diff, untracked list, and porcelain status hashes matched the source workspace
- Implementation owner: primary agent, serially in the source branch workspace

## Quality Gates

- Behavior items: `RED -> verify RED -> GREEN -> verify GREEN -> REFACTOR -> re-verify`
- Backend target tests: focused `go test` commands for each package and behavior
- Backend affected scope: `go test -tags=unit ./...`, relevant `-tags=integration` packages, and targeted `go test -race`
- Backend final gates: `go test ./...`, `go test -tags=unit ./...`, `go test -tags=integration ./...`, `golangci-lint run ./...`, `go build ./cmd/server`
- Frontend target tests: focused `pnpm vitest run` commands
- Frontend final gates: `pnpm test:run`, `pnpm typecheck`, `pnpm lint:check`, `pnpm build`
- Documentation/configuration: applicable format, build, and contract checks only

## Dependency Order and Units

Status values are `pending`, `in_progress`, `completed`, `blocked`, or `failed`.

| Unit | Depends on | Expected outcome | Allowed files | Test/validation | Status | Result / spec check |
|---|---|---|---|---|---|---|
| 1.1 | none | Domain enums, scopes, ladder modes, applicability, defaults | `backend/internal/service/apikey_cooldown*.go`, tests | focused service tests | completed | RED verified missing domain; GREEN and full service package passed; defaults and scope/applicability match proposal/design/spec |
| 1.2 | 1.1 | Validated, normalized, cached runtime setting reads/writes with safe fallback | setting service files, cooldown files, tests | focused service tests | completed | RED verified missing service API; GREEN and full service package passed; valid cache/default fallback/runtime refresh match spec |
| 1.3 | 1.1-1.2 | Admin DTO/handlers/routes without changing legacy 429/529 APIs | admin setting handler/DTO/routes, tests | focused handler/contract tests | completed | RED verified missing handlers; GREEN plus full admin/routes packages passed; separate versioned endpoint added and legacy routes retained |
| 1.4 | 1.1-1.3 | Defaults, override, fallback, refresh, compatibility coverage | backend service/admin tests | focused tests | completed | Focused service/handler tests passed; defaults, override, invalid read/write fallback, runtime refresh, and exact legacy 429/529 values verified |
| 2.1 | 1.1 | Attempt token, observation, decision, and success structures | cooldown domain files, tests | compile/focused tests | completed | RED verified missing contracts; GREEN focused tests passed; structures cover status/content/model/phase/transport/token fields from design |
| 2.2 | 2.1 | Fixed classifier priority and applicability gates | classifier files, tests | classifier table tests | completed | RED verified missing classifier; GREEN priority table passed including scope gate, custom-rule delegation, exclusions, permanent credentials, known and unknown fallback |
| 2.3 | 2.2 | Retry/reset parsing and known family/scope classification | classifier/policy files, tests | classifier table tests | completed | RED verified missing reset parser; GREEN covers Retry-After delta/date, vendor unix/ms/RFC3339 reset, invalid fallback, all known families and dynamic overload scope |
| 2.4 | 1.2,2.3 | Saturating/cycling duration calculation with safe defaults | policy files, tests | policy tests | completed | RED verified missing calculator; GREEN verifies saturation, unknown 1/10/30-minute cycle, override, invalid fallback, disabled policy, and reliable upstream reset |
| 2.5 | 2.2 | Bounded credential-safe summaries | sanitizer/classifier files, tests | sanitizer tests | completed | RED sanitizer tests verified missing sanitizer; GREEN verifies credential redaction, header allowlist, UTF-8 validity, and 512-rune bound |
| 2.6 | 2.2-2.5 | Complete table-driven decision coverage | cooldown tests | focused service tests | completed | Table coverage verifies every default family, scope, ladder mode, priority gate, reset parsing, cycle behavior, and sanitized observation |
| 3.1 | 2.1 | Versioned, group-independent Redis keys and generations | episode store files, tests | store unit tests | completed | Versioned account/model keys omit group IDs; normalized model and failure family isolate streams; generation attempt tokens covered by service and repository tests |
| 3.2 | 3.1,2.4 | Atomic event creation/merge/upgrade | episode store Lua/Go files, tests | Redis integration tests | completed | Redis Lua atomically creates one event, merges active failures without increment, extends later upstream reset monotonically, and advances after expiry; focused tests passed |
| 3.3 | 3.2 | Saturating families and unknown-family cycling | episode store/policy tests | Redis integration tests | completed | Hold-last calculation saturates at the final tier; cross-instance unknown-family test verifies the fourth event returns to tier one |
| 3.4 | 3.2 | Generation-safe success reset across account/model state | episode store files, tests | Redis integration tests | completed | Multi-key success reset clears expired account/model events atomically and rejects active or stale-generation success; memory and Redis tests passed |
| 3.5 | 3.2 | Non-blocking first-tier dependency fallback | store/coordinator files, tests | failure-path tests | completed | Redis/store failure returns a first-tier exclusion and local guard; database persistence failure is logged and retried without advancing streak or blocking the request |
| 3.6 | 3.2-3.5 | 100-way concurrency, multi-instance, isolation, reset coverage | integration tests | Redis integration tests | completed | 100-way merge, cross-instance cycle, family/model isolation, expiry upgrade, reset protection, dynamic TTL, persistence retry, and active reset extension tests passed; targeted race detector passed |
| 4.1 | 2.4 | Monotonic account cooldown through existing snapshot/outbox path | repository/service tests | repository integration tests | completed | Existing conditional write only extends the deadline; no-op writes skip outbox and snapshot updates, while applied writes enqueue account-change outbox and refresh the scheduler snapshot; focused PostgreSQL tests passed |
| 4.2 | 2.4 | Atomic monotonic model cooldown with isolation and propagation | repository/service files, tests | PostgreSQL integration tests | completed | JSONB update compares typed timestamps atomically, preserves later metadata, isolates model keys, refreshes scheduler snapshots, and retained the latest deadline across 100 concurrent writers; focused PostgreSQL tests passed |
| 4.3 | 2.*,3.*,4.1-4.2 | Idempotent coordinator connects classify/event/persist with safe reason | coordinator files, tests | coordinator tests | completed | Coordinator deduplicates attempt IDs, merges events, retries unpersisted events without advancing streaks, dispatches account/model persistence, and stores bounded JSON reasons containing stable family/scope/generation fields; focused service tests passed |
| 4.4 | 3.4,4.3 | Bounded pre-send shared cooldown guard | scheduler/gateway/coordinator files, tests | scheduler integration tests | completed | All covered direct handlers call the shared guard immediately before dispatch, exclude vetoed accounts, preserve generation tokens, fall open on shared-store read errors, and cap guard vetoes separately at 10; handler/service contract and lifecycle tests passed |
| 4.5 | 4.1-4.4 | Cross-group, model isolation, expiry, restart, race coverage | integration tests | PostgreSQL/Redis tests | completed | Added PostgreSQL/Redis integration coverage proving cross-group account exclusion, M1/M2 isolation, automatic expiry, repository rebuild persistence, and a stale scheduler snapshot blocked by a second coordinator instance through the shared guard; all focused tests passed |
| 5.1 | 4.3-4.4 | Exactly one terminal observation per upstream attempt with token | gateway service files, tests | attempt lifecycle tests | completed | Attempt lifecycle carries guard generations, request/response phase, and client context; `sync.Once` admits exactly one success or failure terminal result, including independent WebSocket turns; focused service/handler tests and full packages passed |
| 5.2 | 5.1 | OpenAI/Grok direct API key integration and retry boundaries | OpenAI/Grok gateway files, tests | focused/e2e tests | completed | OpenAI native handlers plus Grok realtime, voice, media, web/X search wrap dispatch with the shared guard and terminal observation; typed HTTP, dial, body-read, transport, empty-response, and success paths preserve existing failover boundaries; contract and package tests passed |
| 5.3 | 5.1 | Anthropic/Gemini/Antigravity and other direct API key integration | gateway service files, tests | focused/e2e tests | completed | Generic gateway attempts cover Anthropic, Gemini, Antigravity, compatible routes, v1beta, token counting, Codex manifest, images, embeddings, and search; observations use each attempt's final routing model and typed upstream status/code/type/body extraction; contract and package tests passed |
| 5.4 | 5.2 | Bypass duplicate in-process model transient state in scope | OpenAI service files, tests | regression tests | completed | Non-pool API key accounts bypass legacy OpenAI process-local model transient writes, reads, and success clears; pool-mode tests retain the legacy behavior. Two stale legacy tests were corrected to construct explicit pool accounts, and the complete service package passed |
| 5.5 | 5.2-5.3 | Explicit exclusions preserve pools/OAuth/setup/other types | classifier/gateway tests | regression tests | completed | `IsAPIKeyFailureCooldownApplicable` limits the coordinator to non-pool `type=apikey`; pool, OAuth, setup token, Live OAuth-only, and other account paths retain their existing retry and health behavior; focused scope tests and complete packages passed |
| 5.6 | 2.2,5.2-5.3 | Actually matched custom rules remain authoritative | gateway/coordinator tests | regression tests | completed | `HandleUpstreamError` computes actual custom-code/temp-rule matches before observation; matched rules delegate without default writes, while enabled-but-unmatched rules continue through known or unknown classification; classifier and existing error-policy regressions passed |
| 5.7 | 4.4,5.2-5.3 | Failover only when replay-safe | gateway service/handler files, tests | failover tests | completed | Failure observation precedes failover selection; client cancellation, downstream output, and non-replay-safe Grok generation stop replay, while pre-output replay-safe failures exclude the account and select the next candidate; guard vetoes are separately bounded at 10; focused failover tests passed |
| 5.8 | 5.1-5.7 | End-to-end permanent/no-cooldown/known/unknown/failover coverage | integration/e2e tests | focused e2e tests | completed | Unit-tagged handler integration covers permanent invalid key delegation, request 400 exclusion, 503 cooldown plus same-request switch, cross-instance unknown 1/10/30/1-minute cycle, stale-snapshot guard, and client-disconnect stop; focused suite plus full handler and service packages passed |
| 6.1 | 1.3 | Frontend types, defaults, normalized API mapping | frontend settings API/types, tests | Vitest API tests | completed | RED verified missing exports; GREEN adds exact version/family types, independent defaults, normalized response mapping, deep-cloned PUT payload, and settings API exposure; 3 focused tests, typecheck, and focused ESLint passed |
| 6.2 | 6.1 | Central settings UI with scopes and all policies | `SettingsView.vue`, extracted components if needed, tests | Vitest component tests | completed | RED verified the component was absent; GREEN adds a compact standalone settings component, all ten policy toggles/ladders/modes, fixed account/model scope labels, complete versioned save, reload control, and gateway-tab integration; 38 focused component/view tests, typecheck, and focused ESLint passed |
| 6.3 | 6.2 | Chinese/English copy, validation, save/error feedback | i18n/settings UI files, tests | Vitest/typecheck | completed | RED verified empty, zero, text, and decimal ladders were submitted; GREEN adds positive-integer comma-list validation, inline accessible errors, submit blocking, retained drafts on server failure, and complete Chinese/English copy; 15 focused component/i18n tests, typecheck, and focused ESLint passed |
| 6.4 | 6.1-6.3 | Default/edit/reject/reload UI/API coverage | frontend tests | focused Vitest | completed | API tests cover exact defaults, GET/PUT paths, normalized ladders, and deep copies; component tests cover all ten defaults/scopes, valid edit/save, four invalid ladder forms, server failure retention, and reload from latest server state; 55 focused API/component/view/i18n tests, typecheck, and focused ESLint passed. Browser screenshot attempt was invalid because the execution environment intercepted both requested localhost ports with an unrelated Nginx application; temporary preview files and server were removed |
| 7.1 | 1-6 | Backend unit and PostgreSQL/Redis integration regression gate | tests/implementation | backend test gates | completed | Proxy-cleared `go test ./...` and `go test -tags=integration ./...` passed. Unit-tagged packages passed after correcting one stale 529 assertion: a direct API key custom-code list containing only 429 does not match 529 and therefore must continue to default 529 cooldown per task 5.6; focused and full unit service package passed. Initial Aliyun captcha local-server timeouts were traced to injected proxy variables and passed unchanged with proxies cleared |
| 7.2 | 3-5 | Race detector validates concurrent event/selection paths | tests/implementation | targeted `go test -race` | completed | Race detector passed for memory event merge, coordinator deduplication/persistence/guard paths, concurrent account runtime stats, Redis 100-way merge and cross-instance cycle, PostgreSQL 100-writer monotonic model cooldown, and stale-snapshot cross-instance guard |
| 7.3 | 6 | Frontend tests and type checking pass | frontend files/tests | frontend gates | completed | Full frontend gates passed: 248 test files and 1778 tests, typecheck, ESLint, and production build. An initial parallel ESLint run observed a just-removed Vitest temporary file; the required serial rerun passed, and the build emitted only the repository's existing chunk-size warning |
| 7.4 | 5 | Legacy 429/529, custom rules, quota, recovery, pools and non-API-key regression | tests/implementation | focused + full regression | completed | Focused service regressions passed for legacy 429/529 settings and behavior, custom-rule hit/miss and existing policy application, OpenAI quota snapshot/runtime-block paths, manual error/rate-limit/temp/model-state recovery, API-key pool retry behavior, and OAuth/setup-token/Bedrock applicability guards. Both normal and unit-tagged focused suites passed with proxy variables cleared where applicable |
| 7.5 | 1-7.4 | Operations guide covers defaults, keys, fallback, diagnostics, rollout | project docs | docs/build validation | completed | Added `docs/API_KEY_FAILURE_COOLDOWN.md` with all ten default families/scopes/ladders/modes, the versioned `version`/`policies`/`enabled`/`cooldowns`/`mode` contract, upstream reset precedence, Redis and dual-dependency fallback behavior, account/model reason inspection, recovery, compatibility, and backend-first no-migration rollout. Required-content, focused implementation tests, `git diff --check`, and migration-diff checks passed |

## Final Transaction

- Full validation: completed — after adding direct passthrough lifecycle boundary coverage, final backend `go test ./...`, `go test -tags=unit ./...`, and `go test -tags=integration ./...` passed with proxy variables cleared; CI-pinned golangci-lint v2.13 passed with `0 issues`; `go build ./cmd/server` output to `/tmp/sub2api-server-build.final-20260828/server` passed; `git diff --check` and strict OpenSpec validation passed; frontend 248 files/1778 tests, typecheck, ESLint, and production build had already passed serially, with only existing dynamic-import/chunk-size warnings
- Final specification review: completed — all requirements map to implementation and tests: applicability is exactly non-pool `type=apikey`; all configured families, fixed scopes, upstream reset precedence, event merging, monotonic account/model persistence, shared pre-send guard, success generation protection, custom-rule priority, compatibility paths, and dependency fallbacks are covered. Replay boundaries preserve client cancellation, already-started output, and non-replay-safe handling. Passthrough ordering is explicitly verified so pre-output rejection runs before any downstream-start marker, dropped frames never mark output, and accepted terminal frames mark response start before completing the cooldown attempt. Live remains OpenAI OAuth-only; pool, OAuth, setup-token, Bedrock, and legacy 429/529 behavior remain outside the new coordinator. Sanitization tests and bounded reasons prevent credential/full-body persistence. No migration files changed, and `docs/API_KEY_FAILURE_COOLDOWN.md` documents defaults, fields, fallback, diagnostics, recovery, compatibility, and no-migration rollout.
- Reusable apply snapshot search: completed — no local branch matched `add-apikey-failure-cooldown-apply-*`, so no reusable snapshot exists
- Local snapshot branch/worktree/commit: completed — created branch `add-apikey-failure-cooldown-apply-20260828122520` from original HEAD in `/home/pan/桌面/code/add-apikey-failure-cooldown-apply-20260828122520`; copied all 52 tracked changes and 41 non-ignored untracked files, force-added ignored `docs/API_KEY_FAILURE_COOLDOWN.md`, and retained one local snapshot commit without pushing
- Binary patch and four-way existence/workspace verification: completed — generated `/home/pan/桌面/code/add-apikey-failure-cooldown-apply-20260828122520.patch`; verified source `main` remains at original HEAD, pre-apply backup branch/worktree exist, snapshot branch/worktree/commit exist with original HEAD as parent and a clean tree, the operations guide is committed, the patch is non-empty, and source tracked-diff/untracked/status hashes remain unchanged

## 2026-08-28 全请求首个有效内容超时增量

### Baseline

- Source branch: `main`
- Original HEAD: `b2215c9e80dab4902aba02414be8ec24e1b196f4`
- Starting workspace: no tracked changes; one untracked file `openspec/changes/add-apikey-failure-cooldown/Untitled`
- Pre-apply backup branch: `add-apikey-failure-cooldown-pre-apply-20260828143358`
- Pre-apply backup worktree: `/home/pan/桌面/code/add-apikey-failure-cooldown-实施前备份-20260828143358`
- Backup verification: tracked diff hash is the empty SHA-256; untracked path and content hashes match the source workspace
- Implementation owner: primary agent, serially in the source branch workspace

### Quality Gates

- Behavior items: `RED -> verify RED -> GREEN -> verify GREEN -> REFACTOR -> re-verify`
- Focused tests: API Key attempt lifecycle, handler response writer, protocol lifecycle/failover, channel monitor checker
- Affected scope: `go test` for service and handler packages plus targeted `go test -race`
- Final gates: `go test ./...`, `go test -tags=unit ./...`, `go test -tags=integration ./...`, `golangci-lint run ./...`, `go build ./cmd/server`, `git diff --check`, strict OpenSpec validation

### Dependency Order and Units

| Unit | Depends on | Expected outcome | Allowed files | Test/validation | Status | Result / spec check |
|---|---|---|---|---|---|---|
| 8.1 | existing 5.1 | Attempt-level 29-second guard, typed timeout, context cancellation, valid-content disarm | `backend/internal/service/apikey_cooldown_attempt*.go`, `ratelimit_service.go`, tests | focused service tests | completed | RED verified missing lifecycle API; GREEN verifies typed timeout cancellation, explicit valid-content disarm, success disarm, and existing attempt lifecycle regressions. The fixed production threshold is 29 seconds and the guard remains attempt-scoped. |
| 8.2 | 8.1 | HTTP/stream/WS valid-content boundaries; timeout maps to account transient cooldown | cooldown classifier/lifecycle, handler writer/hooks, detach helper, tests | focused service/handler tests | completed | RED/GREEN verifies headers, empty writes and SSE comments do not disarm; body/SSE semantic data and WebSocket semantic output do. Timeout is classified as account-scoped `transient_upstream`. |
| 8.3 | 8.1-8.2 | Current attempt cancellation, safe failover, 504 fallback, current-service monitor behavior and 35-second monitor deadline | gateway/handler/channel monitor files and tests | focused integration/contract tests | completed | Typed context cause propagates as retryable account-scoped 504; safe replay remains governed by existing output/replay checks. Monitor response-header deadline is 35 seconds and its current-service request is not exempt. |
| 8.4 | 8.1-8.3 | Affected and full gates, race check, spec review, snapshot | tests/docs/OpenSpec/coordination | final gates | completed | Focused tests plus `go test ./...`, unit and integration tags, and targeted race checks passed. `go vet`, build, diff check and strict OpenSpec validation passed. `golangci-lint` was unavailable in this environment (`command not found`); no repository-provided replacement exists. Review confirms 29-second account-only guard, semantic-output boundary, safe failover/504 behavior, monitor 35-second deadline, no migrations, and documented operations. Snapshot below. |

### 2026-08-28 增量最终事务

- 完整验证：代理变量清除后，`go test ./...`、`go test -tags=unit ./...`、`go test -tags=integration ./...`、相关 `go test -race`、`go vet ./...`、`go build ./cmd/server`、`git diff --check` 与 `openspec validate add-apikey-failure-cooldown --strict` 均通过。
- 静态检查：`golangci-lint run ./...` 未执行，因为环境没有该命令，且 Makefile 未提供替代入口；已以 `go vet ./...` 补充基础静态检查。
- 本地快照：`add-apikey-failure-cooldown-apply-20260828151801`，工作树 `/home/pan/桌面/code/add-apikey-failure-cooldown-apply-20260828151801`，提交 `addd7cc2a`；二进制补丁为 `/home/pan/桌面/code/add-apikey-failure-cooldown-apply-20260828151801.patch`。该提交以原始 HEAD `b2215c9e80dab4902aba02414be8ec24e1b196f4` 为祖先，包含忽略的运维文档和原有未跟踪文件；源工作树保持未提交状态。
