## 1. 冷却配置与领域模型

- [x] 1.1 定义仅适用于非池模式 `type=apikey` 的故障类别、冷却作用域、阶梯策略、未知故障循环策略及默认配置模型。
- [x] 1.2 在系统设置服务中增加统一冷却配置的读取、校验、规范化与保存能力，并为非法配置保留最近一次有效配置或回退到内置默认值。
- [x] 1.3 增加管理端设置接口的数据结构与路由，确保旧的 429/529 冷却配置在迁移后仍按兼容规则生效。
- [x] 1.4 为默认值、配置覆盖、非法值回退、运行时刷新和旧配置兼容补充单元测试。

## 2. 故障观察、分类与策略决策

- [x] 2.1 定义统一的请求尝试标识、故障观察结果、冷却决策和成功确认结构，覆盖 HTTP 状态、上游错误内容、模型、响应阶段与传输错误。
- [x] 2.2 实现故障分类器的优先级：适用范围检查、已命中的账号自定义规则、明确不冷却请求错误、永久凭据错误沿用既有错误处理、已知故障分类、未知故障兜底。
- [x] 2.3 实现 `Retry-After` 与上游重置时间解析，以及 429、529/过载、401、403、配额耗尽、模型不支持、5xx/网络故障、全局上游故障的作用域和故障族判定。
- [x] 2.4 实现可配置的冷却时长计算：可饱和阶梯保持最高档，未知故障按 1、10、30 分钟循环，并在配置不可用时采用内置默认值。
- [x] 2.5 限制并清洗参与分类及落库的上游响应内容，避免密钥、令牌或超长响应进入日志、Redis 或账号状态原因。
- [x] 2.6 为分类优先级、全部默认策略、作用域选择、未知故障循环和响应内容清洗增加表驱动测试。

## 3. 高并发冷却事件状态

- [x] 3.1 设计 Redis 键、事件代次和尝试令牌，使账号级状态跨分组共享、模型级状态按“账号＋模型”共享，并按故障族分别计数。
- [x] 3.2 使用 Redis 原子脚本实现“首次失败开启事件并只计数一次、冷却期内并发失败合并、到期后下一次失败进入下一档”的状态转换。
- [x] 3.3 实现可饱和故障族的最高档保持，以及未知故障族第四次起从第一档重新循环。
- [x] 3.4 实现成功重置：清除账号级连续失败计数及本次账号模型的模型级计数，同时用事件代次阻止旧请求的迟到成功清除新事件。
- [x] 3.5 定义 Redis 不可用、超时或原子脚本失败时的降级行为，保证请求失败处理不会被状态依赖阻塞或重复放大。
- [x] 3.6 增加 Redis 集成测试，验证至少 100 个并发失败只形成一个冷却事件、跨实例一致性、多故障族隔离、到期升级、成功重置与迟到成功保护。

## 4. 冷却状态持久化与调度守卫

- [x] 4.1 复用 `temp_unschedulable_until/reason` 写入账号级冷却，验证既有“只延长不缩短”语义、调度快照同步和 outbox（事务消息表）通知。
- [x] 4.2 将 `extra.model_rate_limits` 的模型级冷却写入改为原子“只延长不缩短”，并补充并发写入、不同模型隔离及调度同步测试。
- [x] 4.3 实现统一冷却协调器，把分类、策略、事件合并和账号/模型状态持久化串成幂等流程，并为原因字段输出可诊断但不含敏感信息的结构化摘要。
- [x] 4.4 在最终选定账号、真正发出上游请求前增加共享冷却状态守卫；命中冷却时排除该账号或账号模型并重新选择，且限制重选次数。
- [x] 4.5 为账号冷却跨所有分组生效、模型冷却不影响同账号其他模型、过期恢复、进程重启恢复和最终调度竞态补充集成测试。

## 5. API Key 请求链路接入

- [x] 5.1 在每次上游尝试结束处生成一次且仅一次的成功或失败观察结果，并传递尝试开始时取得的事件代次令牌。
- [x] 5.2 接入 OpenAI 与 Grok 的非池模式 API Key 请求链路，覆盖 HTTP 错误、连接超时、连接重置、空响应、成功和安全重试边界。
- [x] 5.3 接入 Anthropic、Gemini、Antigravity 及其他现有非池模式 API Key 直连链路，并统一模型标识和上游错误提取方式。
- [x] 5.4 对新协调器已覆盖的非池模式账号停用旧的进程内模型瞬态状态，避免同一失败被重复计数或叠加冷却。
- [x] 5.5 保持 API Key 号池、OAuth、setup token（初始化令牌）及其他账号类型的既有行为不变，并增加明确的适用范围守卫。
- [x] 5.6 保持已命中的账号自定义临时不可调度规则优先；规则未开启或未命中时才执行新的系统默认分类与未知故障兜底。
- [x] 5.7 实现安全故障转移：尚未输出且不存在重复执行风险时切换到分组内下一账号；客户端取消、已开始输出或存在重复执行风险时停止重放。
- [x] 5.8 增加端到端测试，覆盖永久无效密钥沿用账号错误处理、明确不冷却错误、已知冷却、未知循环、同请求故障转移和不可安全重放场景。

## 6. 管理端配置界面

- [x] 6.1 增加前端冷却策略配置类型、默认值和 API 客户端映射，保持与后端字段和校验规则一致。
- [x] 6.2 在系统设置页提供统一冷却策略配置，覆盖启用状态、各故障族阶梯、未知故障循环及全局故障时长，并显示作用域说明。
- [x] 6.3 增加中文和英文界面文案、输入边界校验、保存反馈与服务端错误展示。
- [x] 6.4 增加前端组件和 API 映射测试，验证默认配置展示、编辑保存、非法值阻止提交和重新加载。

## 7. 回归验证与交付

- [x] 7.1 运行后端相关包的单元测试与 PostgreSQL/Redis 集成测试，修复故障分类、事件合并、状态持久化和调度守卫中的回归。
- [x] 7.2 对冷却事件与账号选择的并发路径运行 Go race detector（竞态检测器），验证 100 并发及多实例场景没有重复升级或状态回退。
- [x] 7.3 运行前端类型检查和测试，验证管理端配置可以正常读取、修改并即时生效。
- [x] 7.4 回归现有 429/529、账号自定义规则、配额快照、手动状态恢复、API Key 号池和非 API Key 账号流程，确认不在本次范围内的行为未改变。
- [x] 7.5 补充运维说明，记录默认策略、配置字段、Redis 降级行为、冷却原因查看方式和无需数据库迁移的发布步骤。

## 8. 全请求首个有效内容超时

- [x] 8.1 以 TDD 扩展 API Key 尝试生命周期，增加固定 29 秒首个有效内容守卫、强类型超时原因、上下文取消和有效内容解除语义。
- [x] 8.2 以 TDD 接入 HTTP 非流式、流式及 WebSocket 的有效内容边界，确保响应头、空写入和 SSE 保活不解除守卫，并将超时归入账号级瞬时上游冷却。
- [x] 8.3 以 TDD 验证超时只取消当前账号尝试，安全请求继续故障转移，不安全请求返回 504；当前服务渠道监控不豁免并在 35 秒无响应时判错。
- [x] 8.4 运行后端目标、受影响范围、竞态、静态检查、构建和 OpenSpec 校验，完成最终规格核对与本地快照事务。
