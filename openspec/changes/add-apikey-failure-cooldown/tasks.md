## 1. 配置与数据契约

- [x] 1.1 定义渠道监控冷却配置模型、默认值和服务端校验：五档严格递增分钟数、慢响应阈值、优先级增量、最大增加次数、自动恢复时长。
- [x] 1.2 在系统设置服务和管理路由中实现 GET/PUT 配置接口及恢复默认接口，支持运行时刷新、最近一次有效配置回退和错误响应。
- [x] 1.3 在渠道监控 Ent schema、服务模型、DTO、仓储映射和数据库迁移中增加持久化字段 `use_current_service`，默认值为 false。
- [x] 1.4 为配置默认值、非法配置、运行时刷新、恢复默认和监控开关 CRUD 编写先失败后通过的后端测试。

## 2. 渠道监控表单与请求标记

- [x] 2.1 修改“使用当前服务”按钮，使其同时写入当前 endpoint 和 `use_current_service=true`；手工修改 endpoint 时清除开关，编辑已有监控时保留服务端值。
- [x] 2.2 更新管理端 Channel Monitor API 类型、表单提交和中英文文案，覆盖新增、编辑、复制及加载回填。
- [x] 2.3 实现内部签名监控标记的生成、过期校验和 request context 注入；无效或缺失标记必须按普通请求处理，并在转发前移除。
- [x] 2.4 让 `use_current_service=true` 的 checker 为每个探测请求携带标记，让其他 endpoint 请求不携带标记；补充请求头和伪造标记测试。

## 3. 账号级冷却链

- [x] 3.1 实现仅识别监控 context 的账号尝试 observer，并在账号选择、请求发送、成功、失败和取消出口各调用一次终态观察。
- [x] 3.2 使用 Redis 原子状态实现账号级五档冷却事件：活动窗口并发合并、到期推进、第五档保持、成功清理和迟到结果保护。
- [x] 3.3 在最终发送前增加账号级共享冷却守卫，命中时排除账号并沿用现有安全重试边界；Redis 不可用时旁路且不阻塞请求。
- [x] 3.4 将所有带标记失败统一接入冷却，不再按模型、故障族或 API Key 类型分支；确认普通 API Key、OAuth、Setup Token、号池及其他账号请求完全旁路。
- [x] 3.5 为账号跨分组共享、并发合并、五档推进、成功恢复、进程重启和降级行为编写 Redis/服务集成测试。

## 4. 慢响应优先级调节

- [x] 4.1 在账号扩展状态中定义临时优先级加成结构，使用原子事务更新 boost、增加次数和首次增加时间，并同步调度缓存。
- [x] 4.2 修改调度排序使用“基准优先级 + 临时加成”，确保管理员修改基准值后恢复到新基准值且不改变普通调度语义。
- [x] 4.3 在带标记探测完成后按真实耗时执行加成、上限、正常响应立即恢复和自动恢复计时；自动恢复后下一次超时开启新周期。
- [x] 4.4 为阈值边界、连续三次增加、并发上限、正常响应恢复、一小时自动恢复、重启恢复和基准优先级变更编写服务/仓储/调度测试。

## 5. 管理端配置界面

- [x] 5.1 增加渠道监控冷却设置 API 类型、默认值、加载保存映射和恢复默认调用。
- [x] 5.2 在管理设置页增加冷却阶梯、慢响应阈值、优先级增量、最大增加次数和自动恢复时长表单，展示单位并阻止非法提交。
- [x] 5.3 增加中文和英文文案、保存/加载/恢复默认反馈以及服务端错误展示。
- [x] 5.4 编写前端 API、组件和页面测试，覆盖默认值、编辑保存、恢复默认、非法值阻止和重新加载。

## 6. 集成验证与发布

- [x] 6.1 运行后端受影响包测试、数据库迁移测试、Redis 并发测试和竞态检测，修复跨模块回归。
- [x] 6.2 回归普通 API Key、API Key 号池、OAuth、Setup Token、客户端取消、现有 429/529 处理和非当前服务监控，确认不进入新冷却链。
- [x] 6.3 运行前端类型检查、测试、静态检查和构建，确认设置可读取、修改并即时生效。
- [x] 6.4 运行完整测试、构建、OpenSpec 严格校验和 `git diff --check`，补充运维说明并完成最终规格核对。
