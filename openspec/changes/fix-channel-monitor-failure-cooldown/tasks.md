## 1. 请求账本契约与 Redis 存储

- [x] 1.1 定义渠道监控请求账本模型和接口，包含 monitor ID、request ID、目标 API Key 账号尝试、传输终态、观测到的冷却代次、最终归因状态和有限 TTL。
- [x] 1.2 实现 Redis 账本键与原子脚本，支持登记/更新账号尝试、失败终态优先、请求完成标记、最终归因锁定及重复完成幂等。
- [x] 1.3 为账本存储补充 Redis 测试，覆盖多账号换号、同账号重复尝试、主/附加模型隔离、并发终态更新、TTL 清理和重复最终归因。
- [x] 1.4 明确请求级 Redis 失败状态：任一步读写失败后不使用进程内持久化兜底，后续归因只接受完整可读取的账本，并记录不含凭据的结构化错误日志。

## 2. 网关账号尝试采集

- [x] 2.1 改造 `ChannelMonitorProbeObserver`，继续校验可信监控 context，并只登记普通 API Key 且非号池账号；OAuth、Setup Token、Service Account 和号池账号保持旁路。
- [x] 2.2 让 Gateway 与 OpenAI Gateway 的各协议账号选择/转发出口把每次实际尝试及 `transport_failed`、`transport_succeeded` 或未知终态写入同一 request ID 账本。
- [x] 2.3 调整成功处理时机：网关传输成功不得在 checker 最终结果前清理冷却；同一账号本次请求内出现失败后再成功时，失败终态必须保留。
- [x] 2.4 补充网关服务与 handler 测试，覆盖 Chat Completions、Responses、流式路径、换号、客户端取消、无账号失败以及所有非目标账号旁路。

## 3. Checker 最终结果归因

- [x] 3.1 在每个模型独立生成签名标记时保存对应 request ID，并在 `runCheckForModel` 返回最终 `CheckResult` 后调用统一归因器；主模型与附加模型不得共享账本。
- [x] 3.2 实现最终 `failed/error` 归因：读取完整账本并对其中所有实际尝试过的目标 API Key 账号各执行一次现有 `ObserveFailure`，包括网关 2xx 后挑战失败、空响应和客户端超时。
- [x] 3.3 实现非失败结果归因：所有传输失败账号仍执行失败冷却，仅传输成功且没有同请求失败的账号按观测代次执行现有成功清理；未知终态不清理。
- [x] 3.4 保持慢响应优先级调整的现有参数和行为，只把其观察结果挂到最终归因后的正确账号，不修改管理端设置契约。
- [x] 3.5 补充 checker/服务测试，覆盖最终 2xx 挑战失败、replace 模式空响应、超时、前序失败后续成功、所有尝试最终失败、重复回调和不同模型隔离。

## 4. 冷却规则、状态投影与故障降级

- [x] 4.1 回归现有账号级冷却存储，确认活动冷却并发合并、到期推进、末档保持、成功清理连续失败计数、活动截止时间不缩短和迟到成功代次保护。
- [x] 4.2 确认账号调度在所有分组中继续排除命中 `channel_monitor:cooldown:account:<id>` 的账号，且不新增模型、分组或错误族维度。
- [x] 4.3 补充管理端账号列表和详情测试，确认新归因产生的状态显示现有 `channel_monitor_cooldown` 原因，并且其他冷却来源不被覆盖。
- [x] 4.4 增加 Redis 不可用回归测试，分别模拟尝试登记和最终归因失败，确认本次请求不写入任何冷却、不启用进程内兜底、不暂停监控、不阻塞原始响应，并产生结构化诊断日志。

## 5. 集成验证

- [x] 5.1 运行后端受影响包测试和 Redis 集成测试，验证所有网关协议都能把最终 `CheckResult` 与实际账号尝试关联。
- [x] 5.2 回归普通 API Key 请求、未使用当前服务的监控、OAuth、Setup Token、Service Account、号池账号及没有选中账号的失败，确认均不进入本冷却链。
- [x] 5.3 验证管理端现有冷却阶梯设置无需迁移即可即时生效，且慢响应优先级行为和页面没有变化。
- [x] 5.4 运行完整测试、构建、竞态检查、OpenSpec 严格校验和 `git diff --check`，核对实现与本规格所有场景一致。
